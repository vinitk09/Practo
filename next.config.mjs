/** @type {import('next').NextConfig} */
const nextConfig = {

     images: {
    domains: ['images.unsplash.com','thumbs.dreamstime.com','images.rawpixel.com','source.unsplash.com'],
  },
};

export default nextConfig;
